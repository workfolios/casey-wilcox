# Stage One Preview Branch Publication Trigger

This marker records the sponsor-authorized attempt to publish the validated static build to the `gh-pages` branch while search-engine indexing remains disabled.
